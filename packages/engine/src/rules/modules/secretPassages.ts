/**
 * secretPassages.ts — five mansion teleport nodes (RFC 007 Phase 3).
 */

import type { RuleModule } from '../RuleModule.js';

export const secretPassagesModule: RuleModule = {
  id:          'SECRET_PASSAGES',
  displayName: 'Secret passages',
  description: 'Five linked teleport spaces — hop between any two for 1 pip.',
};
